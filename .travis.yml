language: node_js
node_js:
  -11.6.0
cache: yarn

install:
  - yarn install
script:
  - node updatePwa.js

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: gh-pages